# How to run example

Generate SQL:

```
./example_airports > airports.sql 
```

Execute script in a PostgreSQL database that contains the openflights_airports table (see *data* directory). This creates a table called *openflights_airports_gen*.

 
